import { TitreDirective } from './titre.directive';

describe('TitreDirective', () => {
  it('should create an instance', () => {
    const directive = new TitreDirective();
    expect(directive).toBeTruthy();
  });
});
