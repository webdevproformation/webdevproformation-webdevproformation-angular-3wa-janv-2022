import { ParagraphDirective } from './paragraph.directive';

describe('ParagraphDirective', () => {
  it('should create an instance', () => {
    const directive = new ParagraphDirective();
    expect(directive).toBeTruthy();
  });
});
